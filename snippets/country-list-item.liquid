{%- comment -%}
  Renders a single country list item

  Page-weight note: no per-country flag image and no per-country inline SVG.
  Only the currently selected country renders a checkmark; other rows get an
  empty placeholder span so the grid columns stay aligned.

  Accepts:
    - country: country object
    - show_currencies: boolean to show/hide currency
    - current_country: the currently selected country object
{%- endcomment -%}

{%- liquid
  assign is_current = false
  if country.iso_code == current_country.iso_code
    assign is_current = true
  endif
-%}
<li class="disclosure__item" tabindex="-1">
  <a
    class="link link--text disclosure__link caption-large focus-inset"
    href="#"
    role="button"
    {% if is_current %}
      aria-current="true"
    {% endif %}
    data-value="{{ country.iso_code }}"
    id="{{ country.name | handleize }}-{{ country.iso_code }}"
  >
    <span class="localization-check{% unless is_current %} visibility-hidden{% endunless %}" aria-hidden="true">
      {%- if is_current -%}
        {{- 'icon-checkmark.svg' | inline_asset_content -}}
      {%- endif -%}
    </span>
    <span class="country">
      <span class="country-name">{{- country.name -}}</span>
    </span>
    <span class="localization-form__currency motion-reduce{% unless show_currencies %} hidden{% endunless %}">
      {{ country.currency.iso_code }}
      {{ country.currency.symbol -}}
    </span>
  </a>
</li>
